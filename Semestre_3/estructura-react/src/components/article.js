import React from "react";
import { Section } from "./section";

export const Article = (props) =>{
    return(
        <article>
            <Section/>
            <Section/>
            <Section/>
            <Section/>
        </article>
    );
}